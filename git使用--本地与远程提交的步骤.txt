1，提交步骤(从本地提交至远程)
创建文件，将文件加入暂存区		  拉取远程仓库最新代码	     将暂存区文件加入本地仓库	    向远程仓库提交最新代码
   git add '文件名'	 		--->          git pull	    --->  git commit -m 'log日志'  --->	  		git push
	

2，拉取步骤(从远程拉取至本地)
  克隆远程仓库项目		    拉取远程最新代码
git clone 远程仓库url --->	   git pull
